# -*- coding: utf-8 -*-
"""Mumbai House_Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xmYxr-wwgm_eFqYFjW9CEtmrr5WBX4wA
"""

import streamlit as st
import pandas as pd
import joblib

# Load trained pipeline model
model = joblib.load("mumbai_house_model.pkl")
encoder = joblib.load("label_encoder.pkl")

st.title("Mumbai House Price Prediction")

st.write("Enter property details")

area = st.number_input("Area (sqft)", 200, 5000)
bedrooms = st.number_input("Bedrooms", 1, 10)
bathrooms = st.number_input("Bathrooms", 1, 10)
parking = st.number_input("Parking Spaces", 0, 5)

location = st.selectbox(
    "Location",
    [
        "Andheri West",
        "Bandra",
        "Borivali",
        "Thane",
        "Navi Mumbai"
    ]
)

# Create dataframe
df = pd.DataFrame({
    "area": [area],
    "bedrooms": [bedrooms],
    "bathrooms": [bathrooms],
    "parking": [parking],
    "location": encoder.transform([location])
})

if st.button("Predict Price"):
    prediction = model.predict(df)
    st.success(f"Estimated Price: ₹ {prediction[0]:,.2f}")
